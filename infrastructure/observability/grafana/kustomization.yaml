apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: observability

# Grafana is deployed as part of kube-prometheus-stack
# This directory can be used for custom dashboards and configuration
